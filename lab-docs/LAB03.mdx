---
title: Laboratório 03
sidebar_position: 6
---

import LabTable from "@site/src/components/LabTable";
import LabTeamMembers from "@site/src/components/LabTeamMembers";

# Laboratório 03

{/* Tabela com link para atividade, inicio, fim e descrição do LAB! */}

<div style={{ display: "flex", justifyContent: "center" }}>
  <LabTable index={3} internal={false} />
</div>

---

## Checklist — Espressif 32: development platform for PlatformIO

- [ ] Verificar `git --version`
- [ ] Abrir o VS Code
- [ ] Abrir a extensão PlatformIO IDE
- [ ] Instalar a extensão Wokwi
- [ ] Ativar/configurar o Wokwi
- [ ] Criar um projeto ESP32
- [ ] Selecionar o framework Arduino
- [ ] Executar o primeiro Build
- [ ] Criar `wokwi.toml`
- [ ] Criar `diagram.json`
- [ ] Executar a simulação
- [ ] Verificar o Serial Monitor
- [ ] Atualizar o repositório no GitHub
- [ ] Executar `git push`
- [ ] Confirmar que o projeto está disponível no GitHub
- [ ] Logout

---

## Configuração do ambiente de desenvolvimento

import { DevTools } from "@site/src/components/InstructionsSite";

<DevTools />

Verifique o seu ambiente de desenvolvimento, git, gh e code:

import { VerifyDev1 } from "@site/src/components/InstructionsSite";

<VerifyDev1 />

Configure o git

import { GitConfig } from "@site/src/components/InstructionsSite";

<GitConfig />

- [Git Cheat Sheet](https://training.github.com/downloads/github-git-cheat-sheet.pdf)

## Clone o repositório inicial do LAB03

Escolha o Grupo e entre com o comando abaixo para criar o repositório no GitHub:

<LabTeamMembers labName="lab03" />

import LabSubmit from "@site/src/components/LabSubmit";

## Envie o link do repositório do seu grupo no Moodle:

<LabSubmit labName="lab03" />

---

## Espressif 32

- [arduino-esp32](https://github.com/espressif/arduino-esp32)
- [Espressif 32](https://docs.platformio.org/en/latest/platforms/espressif32.html)
- [platform-espressif32](https://github.com/platformio/platform-espressif32)
- [platformio/espressif32](https://registry.platformio.org/platforms/platformio/espressif32)

### Libraries

## Arduino Documentation

### Documentação de Referência da Linguagem Arduino

- https://docs.arduino.cc/language-reference/pt/

Funções
Variáveis
Estruturas

## Para controlar a placa Arduino e realizar computações.

- Entradas e Saídas Digitais
  digitalRead()
  digitalWrite()
  pinMode()

---

- Funções Matemáticas
  abs()
  constrain()
  map()
  max()
  min()
  pow()
  sq()
  sqrt()

---

- Bits and Bytes
  bit()
  bitClear()
  bitRead()
  bitSet()
  bitWrite()
  highByte()
  lowByte()

## “platformio.ini” (Project Configuration File)

- https://docs.platformio.org/en/latest/projectconf/index.html

## ESP32 Arduino Hal Logging

Aqui está um exemplo completo para o arquivo `src/main.cpp`.

Este código demonstra a diferença entre as macros nativas do **ESP32 Arduino Hal Logging** (`log_i`, `log_w`, `log_e`, etc.) e o método tradicional `Serial.println()`, além de realizar o piscar de LED (_blink_) assíncrono utilizando `millis()`.

---

### Código: `src/main.cpp`

```cpp
#include <Arduino.h>

// Definição do pino do LED (GPIO 2 é o LED embutido na maioria das placas ESP32 DevKit)
#define LED_PIN 2

// Intervalo de tempo para o Blink (em milissegundos)
const unsigned long BLINK_INTERVAL = 500;

// Variáveis de controle de tempo
unsigned long previousMillis = 0;
bool ledState = LOW;
uint32_t counter = 0;

void setup() {
    // Configura o pino do LED como saída
    pinMode(LED_PIN, OUTPUT);

    // Inicializa a comunicação serial a 115200 baud
    Serial.begin(115200);

    // Aguarda um instante para a estabilização da conexão Serial
    delay(1000);

    Serial.println("\n==============================================");
    Serial.println("   ESP32 Lab: Teste de Logs e Blink de LED   ");
    Serial.println("==============================================\n");

    // Mensagens de Log do ESP32 (requer CORE_DEBUG_LEVEL > 0 no platformio.ini)
    log_v("Log de Verbose: Informação detalhada/baixa relevância.");
    log_d("Log de Debug: Informação para depuração do sistema.");
    log_i("Log de Info: Sistema inicializado com sucesso no pino GPIO %d.", LED_PIN);
    log_w("Log de Warning: Este é um exemplo de aviso!");
    log_e("Log de Error: Este é um exemplo de erro simulação!");
}

void loop() {
    unsigned long currentMillis = millis();

    // Lógica de tempo não-bloqueante (evita o uso de delay)
    if (currentMillis - previousMillis >= BLINK_INTERVAL) {
        previousMillis = currentMillis;

        // Inverte o estado do LED
        ledState = !ledState;
        digitalWrite(LED_PIN, ledState);

        counter++;

        // Mensagem via Serial.println padrão do Arduino
        Serial.print("[Arduino Serial] Ciclo do LED: ");
        Serial.print(counter);
        Serial.print(" | Estado atual: ");
        Serial.println(ledState ? "LIGADO" : "DESLIGADO");

        // Mensagem usando o Log nativo do ESP32 a cada 5 trocas de estado
        if (counter % 5 == 0) {
            log_i("Contador atingiu %u trocas de estado.", counter);
        }
    }
}

```

---

### Destaques do Código

1. **`log_i()`, `log_w()`, `log_e()**`:

- São macros de log nativas do core ESP32/Arduino.
- Suportam formatação similar ao `printf` (ex: `%d`, `%s`, `%u`).
- Adicionam automaticamente a tag do arquivo, linha da chamada e cor (se a flag `-D CONFIG_ARDUHAL_LOG_COLORS=1` estiver no `platformio.ini`).

2. **Uso de `millis()` em vez de `delay()**`:

- O uso de `delay()` bloqueia o processamento do ESP32. Utilizar a verificação por `millis()` permite que o microcontrolador execute outras tarefas de segundo plano no `loop()` sem travar a CPU.

---

```cpp
#include <Arduino.h>

void setup() {
    Serial.begin(115200);
    delay(1000);

    #ifdef DEBUG_MODE
        log_i("SISTEMA INICIADO EM MODO DE DEBUG");
        log_d("Informações detalhadas de hardware e memória ativas.");
    #else
        // Código executado apenas no ambiente Release
        Serial.println("Sistema iniciado (Modo Release).");
    #endif
}

void loop() {
    // Lógica do programa
}
```

## A Internet na IoT

**Ementa:** Modelo de Referência OSI · Arquitetura TCP/IP e camadas

**Stack da semana:** ESP32 · `WiFi.h` · `HTTPClient` · Wokwi

| Aula | Tipo | Conteúdo                                          | Detalhamento                                                                                   |
| ---- | ---- | ------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| 5    | AT   | Modelo OSI: 7 camadas e suas funções              | Encapsulamento de dados, PDUs, papéis de cada camada em redes IoT                              |
| 6    | AT   | Arquitetura TCP/IP e endereçamento IP (IPv4/IPv6) | Subnetting básico, NAT, por que IPv6 é essencial para IoT em escala                            |
| 7    | AP   | ESP32 conectado ao Wi-Fi via Wokwi                | Biblioteca `WiFi.h`, scan de redes, conexão, leitura de IP e RSSI via serial                   |
| 8    | AP   | Requisição HTTP GET com ESP32                     | Biblioteca `HTTPClient`, consumo de API REST pública (ex: `worldtimeapi.org`), parsing simples |

---

- **Objetivo:** Configuração de rede e conectividade.
- **Atividades:**
  1. Criar projeto PlatformIO com suporte Wi-Fi.
  2. Programar conexão Wi-Fi (`WiFi.h`) e exibir IP/MAC/RSSI.
  3. Simular no Wokwi com configuração de rede.
  4. Testar ping e conexão com o broker Mosquitto local.
- **Entrega:** Código `main.cpp` comentado + screenshot do Serial Monitor.

## Sensores e Atuadores em IoT

**Ementa:** Sensores IoT · Atuadores IoT · Integração com microcontroladores

**Stack da semana:** ESP32 · `DHTesp` · Wokwi · PWM · I²C

| Aula | Tipo | Conteúdo                                                                 | Detalhamento                                                                               |
| ---- | ---- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------ |
| 9    | AT   | Tipos de sensores IoT: temperatura, umidade, pressão, corrente, vibração | Princípio de funcionamento, interfaces (I²C, SPI, 1-Wire, analógico), critérios de seleção |
| 10   | AT   | Atuadores: relés, motores, válvulas e inversores                         | Controle por PWM, acionamento de cargas, segurança elétrica e isolação                     |
| 11   | AP   | Sensor DHT22 no Wokwi: leitura de temp. e umidade                        | Biblioteca `DHTesp`, leitura periódica, exibição no monitor serial e no display OLED       |
| 12   | AP   | Controle de LED por PWM + sensor LDR (fotoresistor)                      | `analogRead`, `analogWrite` (LEDC), controle de brilho proporcional à luminosidade         |

---

- **Objetivo:** Leitura de sensores e controle de atuadores.
- **Atividades:**
  1. Montar no Wokwi: ESP32 + DHT22 + MPU6050 (ou sensor de vibração) + LED/Relé.
  2. Programar no PlatformIO a leitura dos sensores.
  3. Controlar atuador (ex: LED ou buzzer).
  4. Exibir dados no Serial Monitor.
- **Entrega:** Diagrama Wokwi + código + valores lidos.
